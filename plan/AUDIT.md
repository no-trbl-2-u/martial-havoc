# Site audit

> Latest findings from `/iterate audit`. Rewritten on each audit
> pass.

(empty — run `/iterate audit` to populate)

## Adoption (2026-09-05)

> Written by `scripts/adopt.mjs`. Each row is a placeholder the
> manifest did not resolve. Fill it in the named files (or in
> `nexus.adopt.json` and re-run adopt on a clean checkout), then
> delete the row.

- [needs-user-call] <HOSTING_URL> unresolved in 3 file(s): skills/critique.md, .claude/agents/reader.md, agents.md
